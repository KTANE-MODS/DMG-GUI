import { Mission } from "./types";

export function generateDMGText(mission: Mission) {
    let missionStr = ""

    // todo just do the first bomb for now, will get others later

}

function generateBombTime(timeSeconds: Number) {
}